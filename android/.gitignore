*.iml
.gradle
/local.properties
/.idea
/build
/captures
.externalNativeBuild
.cxx
app/build

# Proprietary merchant SDKs are installed locally and are not published to Git.
/app/libs/*.aar
/gateway-repo/

# Release signing material must never be committed.
*.jks
*.keystore
keystore.properties
